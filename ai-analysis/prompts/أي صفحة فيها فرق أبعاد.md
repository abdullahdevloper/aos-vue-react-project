Before marking this slice ready for approval, perform a strict visual scale and spacing comparison against the Vue page.

Active route:
[PUT ROUTE HERE]

Compare the running Vue page and React page at the same viewport width, browser zoom, sidebar state, and scroll position.

Check and fix:
- typography scale
- heading size and weight
- body text size and line-height
- content container width
- card width and height
- card padding
- section spacing
- alert/list/card heights
- button height, padding, hover, active, and pressed states
- select/input height, label position, value alignment, and icon alignment
- icon sizes
- chart/media dimensions if present
- shadows, background, and surface colors

Important:
Do not rely on generic MUI defaults.
Override MUI sizing and states locally to match Vue/Vuse.

Do not implement any new section.
Do not touch approved sections unless required for shared layout and document why.

After fixes:
- run npm run build inside react-dashboard-template
- update migration-docs/phase-report.md with a visual comparison checklist
- keep the slice pending user visual approval