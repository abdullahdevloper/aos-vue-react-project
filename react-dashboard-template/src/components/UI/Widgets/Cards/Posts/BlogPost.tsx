import { ContentCard, StatusChips } from "../../../../common";

export default function BlogPost() {
  return <ContentCard title="Migration Notes"><StatusChips labels={["React", "Vuetify parity", "Dashboard"]} /></ContentCard>;
}
